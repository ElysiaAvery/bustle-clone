<div class="container">
  <div class="row">
    <div class="col-sm-1"></div>
    <div class="col-sm-10">
      {{#each model as |article|}}
        {{article-tile article=article}}
      {{/each}}
    </div>
    <div class="col-sm-1">{{new-article saveArticle2="saveArticle3"}}</div>
  </div>
</div>
